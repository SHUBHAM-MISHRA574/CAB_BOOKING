# Backend API base URL - must match the port your server runs on (server default: 5000)
# If you get "fetch failed", set this to http://localhost:5000 (or your server port)
VITE_API_URL=http://localhost:5000
